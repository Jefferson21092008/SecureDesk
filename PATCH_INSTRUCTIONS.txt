SecureDesk V0.4.5 - Input hardening, password policy and secrets

Base expected: V0.4.4 security audit logging merged into main.
No Alembic migration is added in this patch; head remains 0012_add_security_audit_logs.

Suggested workflow:
  git checkout main
  git pull origin main
  git checkout -b security/input-hardening

Extract this patch at the SecureDesk repository root, replacing files when prompted.

Then validate:
  docker compose down
  docker compose up --build -d
  docker compose exec api alembic current
  docker compose exec api pytest -q
  docker compose exec api ruff check .

Expected Alembic head:
  0012_add_security_audit_logs (head)

Do not use `docker compose down -v` unless you intentionally want to delete persistent data.
