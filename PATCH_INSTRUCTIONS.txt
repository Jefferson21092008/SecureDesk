SecureDesk V0.2 - Patch 0005: ticket lifecycle

1. Extraia este patch na raiz do projeto SecureDesk, substituindo os arquivos existentes.
2. Nao use docker compose down -v.
3. Rode:
   docker compose down
   docker compose up --build -d
4. Confira:
   docker compose exec api alembic current
   docker compose exec api pytest -q
   docker compose exec api ruff check .

Esperado:
- migration: 0005_ticket_lifecycle (head)
- testes: 38 passed
- Ruff: All checks passed!
