SecureDesk V0.5.1 — Metrics overview

Base: V0.4.0 already merged into main and tagged.

1. Create branch:
   git checkout main
   git pull origin main
   git checkout -b feat/metrics-overview

2. Extract this patch at the SecureDesk repository root and replace files.

3. Rebuild and validate:
   docker compose down
   docker compose up --build -d
   docker compose exec api alembic current
   docker compose exec api pytest -q
   docker compose exec api ruff check .

Expected Alembic head:
   0012_add_security_audit_logs (head)

No migration is added by V0.5.1.

4. Quick API check in Swagger:
   GET /metrics/overview

Expected authorization scope:
   USER        -> OWN (only the user's tickets)
   AGENT/ADMIN -> GLOBAL (all tickets)

5. Commit:
   git add .
   git commit -m "feat: add ticket metrics overview"
   git push -u origin feat/metrics-overview
