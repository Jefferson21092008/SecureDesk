SecureDesk — V1.0.1 OpenAPI/Swagger

1. Crie a branch:
   git checkout main
   git pull origin main
   git checkout -b docs/openapi-polish

2. Extraia este patch na raiz do projeto, substituindo os arquivos existentes.

3. Reconstrua e valide:
   docker compose down
   docker compose up --build -d
   docker compose exec api alembic current
   docker compose exec api pytest -q
   docker compose exec api ruff check .

Resultado esperado:
   0012_add_security_audit_logs (head)
   202 passed
   All checks passed!

4. Verifique a documentação no navegador:
   http://localhost:8000/
   http://localhost:8000/docs
   http://localhost:8000/redoc
   http://localhost:8000/openapi.json

5. Commit e push:
   git add .
   git commit -m "docs: polish OpenAPI and Swagger"
   git push -u origin docs/openapi-polish

Não há migration nova nesta etapa.
