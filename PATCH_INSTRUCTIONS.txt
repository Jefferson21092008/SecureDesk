SecureDesk V0.4.6 — Vulnerability analysis / final security review

Base: V0.4.5 already merged into main.

1. Create branch:
   git checkout main
   git pull origin main
   git checkout -b security/vulnerability-analysis

2. Extract this patch at the SecureDesk repository root and replace files.

3. Rebuild and validate:
   docker compose down
   docker compose up --build -d
   docker compose exec api alembic current
   docker compose exec api pytest -q
   docker compose exec api ruff check .

Expected Alembic head:
   0012_add_security_audit_logs (head)

No migration is added by V0.4.6.

4. Commit:
   git add .
   git commit -m "security: complete vulnerability analysis"
   git push -u origin security/vulnerability-analysis

The V0.4 milestone can be tagged v0.4.0 after the PR is merged and CI is green.
